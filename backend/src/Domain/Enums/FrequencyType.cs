namespace ChoreWars.Domain.Enums;

public enum FrequencyType
{
    DAILY = 0,
    EVERY_X_DAYS = 1,
    WEEKLY = 2,
    X_TIMES_PER_WEEK = 3,
    SPECIFIC_DAYS = 4,
    MONTHLY = 5,
    EVERY_X_MONTHS = 6,
    YEARLY = 7,
    CUSTOM = 8
}
